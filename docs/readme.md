Documentation generated by workflows:

* Compiling the Wiki contents (if any) into standalone markdown, LaTeX, or PDF documents. Uses Pandoc libraries.
* Publishing the ontology in a human-readable form, using WIDOCO  
